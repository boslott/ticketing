name: tests

on:
  pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: export NPM_TOKEN=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 && cd auth && npm install && npm run test:ci
